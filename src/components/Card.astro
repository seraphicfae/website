<main class="card">
  <img src="/assets/banner.gif" class="banner" alt="Banner" />
  <img src="/assets/pfp.jpg" alt="Profile" class="pfp" />

  <div class="heading">
    <h1>Lumi</h1>
    <p>@lumina.dev • she/her</p>
  </div>

  <section class="info">
    <p><i class="fas fa-cake-candles icon"></i> Minor</p>
    <p><i class="fas fa-location-dot icon"></i> North America</p>
  </section>

  <section class="description">
    <p>
      Self-taught frontend developer with 4 years of on and off experience.
      Psst, I use arch, btw.
    </p>
  </section>
</main>

<style>
html,
body {
	margin: 0;
	height: 100%;
	overflow: hidden;
}

.card {
	background: var(--card-bg);
	color: var(--text);
	border-radius: 10px;
	width: 100%;
	max-width: 450px;
	margin: 0 auto;
	padding-bottom: 2rem;
	box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
	overflow: hidden;
	position: relative;
	text-align: center;
	padding: 0em !important;
	animation: float 4s ease-in-out infinite;
}

.banner {
	width: 100%;
	height: 120px;
	object-fit: cover;
	object-position: center;
	display: block;
	border-top-left-radius: 10px;
	border-top-right-radius: 10px;
	padding: 0em 0em !important;
}

.pfp {
	width: 100px;
	height: 100px;
	border-radius: 50%;
	border: 3px solid var(--accent);
	position: absolute;
	top: 60px;
	left: 17%;
	transform: translateX(-50%);
	background: var(--card-bg);
}

.heading {
	text-align: left;
	padding-left: 20px;
	margin-top: 50px;
}

.heading h1 {
	font-size: 1.5rem;
	margin: 0;
	color: var(--accent);
}

.heading p {
	font-size: 0.9rem;
	margin: 4px 0 0;
	color: var(--text);
	opacity: 0.8;
}

.info,
.description {
	text-align: left;
	padding: 0 20px;
	margin-top: 20px;
	color: var(--text);
}

@media only screen and (min-width: 720px) {
	.card {
		width: 450px;
	}

	.pfp {
		left: 14%;
	}
}

@keyframes float {

	0%,
	100% {
		transform: translateY(20px);
	}

	50% {
		transform: translateY(0px);
	}
}
</style>
